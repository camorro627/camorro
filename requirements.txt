# ---- النقل والتخفي (TLS impersonation + mesh) ----
curl_cffi>=0.7.2            # تقليد بصمة TLS الكاملة لمتصفحات حقيقية (JA3/JA4/H2)
aiohttp>=3.9.5              # عميل غير متزامن لفحوص الصحة والزحف
aiohttp-socks>=0.9.0        # بروكسي SOCKS4/5
scapy>=2.5.0                # التقاط بصمات حقيقية من متصفح تحكم به (harvest)

# ---- التشفير وإدارة الحالة ----
cryptography>=42.0.8        # AES-256-GCM / ChaCha20-Poly1305 / Argon2id
aiosqlite>=0.20.0           # قاعدة حالة الهجوم غير المتزامنة

# ---- الإعدادات والواجهة ----
PyYAML>=6.0.1
rich>=13.7.1                # لوحة التحكم الحية
orjson>=3.10.0

# ---- اختياري: فحص DOM XSS عبر متصفح حقيقي ----
# playwright>=1.45.0
